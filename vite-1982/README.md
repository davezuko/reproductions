## Instructions

```sh
git clone https://github.com/davezuko/reproductions
cd vite-1982
yarn install
yarn dev

# open localhost:3000
# observe the failed request for file.svg when on Windows
# the request works fine if file.svg is within vite's "root"
```